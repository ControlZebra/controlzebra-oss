/**
 * Archived module: analytics hooks were removed because there are no active consumers.
 *
 * If needed in a future milestone, reintroduce narrowly scoped hooks with a clear owner.
 */
export {};
