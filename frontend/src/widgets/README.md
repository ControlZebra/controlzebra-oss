# widgets

Composed UI blocks that combine components and feature/domain data.

## Ownership
- Reusable screen sections
- Dashboard/panel compositions
- Higher-level UI assemblies

## Rules
- Widgets may consume `features` and `shared`
- Widgets should avoid owning domain logic directly
