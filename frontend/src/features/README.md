# features

User-facing feature modules composed from domain + shared building blocks.

## Ownership
- Feature workflows
- Feature-local state and orchestration
- Feature-specific adapters for UI layers

## Rules
- Keep feature boundaries explicit
- Reuse `domain` and `shared` rather than duplicating logic
